dummy IRC server to announce a static message to clients.

Useful when phasing out a server, changing ports, etc.

Usage:

$ ruby ircd-announce.rb "This server is now closed. Please use irc.foo.bar instead."
